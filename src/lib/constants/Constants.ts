const CAMERA_SETTINGS = {
  fov: 50,
  near: 0.1,
  far: 1000,
  aspect: window.innerWidth / window.innerHeight,
  position: { x: 0, y: 15, z: 20 },
  lookAt: { x: 0, y: 5, z: 0 },
};

export { CAMERA_SETTINGS };
